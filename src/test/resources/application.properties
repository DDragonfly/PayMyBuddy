# === Test profile: database séparé ===
spring.application.name=PayMyBuddy-Test

# Base de test (séparée par la prod locale)
spring.datasource.url=jdbc:mysql://localhost:3306/paymybuddy_test?serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=DESYDESY

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=none

# === Charger schema + data ===
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:database/schema.sql
spring.sql.init.data-locations=classpath:database/data.sql

# === Flyway non active pour les tests ===
spring.flyway.enabled=false
